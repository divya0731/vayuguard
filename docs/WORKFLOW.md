# VayuGuard Git Workflow

feature branch → dev → main

Rules:

1. Never push directly to main
2. Work only in feature branches
3. Create Pull Requests for all changes
4. Get review before merging
5. Keep commit messages meaningful

Example Flow:

feature-login → Pull Request → dev → main
